# third-party clone kept only as a local reference; our changes are in
# capture-analysis/fork-pro-backend.diff
fork/
# build output
src/*/bin/
src/*/obj/

# Local working material, never shipped: personal Windows exports, USB capture
# analysis, third-party clones, caches.
windows-export/
capture-analysis/
fork/
__pycache__/

# debhelper build artifacts (dpkg-buildpackage; removed by debian/rules clean)
debian/.debhelper/
debian/openxlr/
debian/out/
debian/files
debian/debhelper-build-stamp
debian/*.debhelper
debian/*.debhelper.log
debian/*.substvars

# PPA source packages and the vendored NuGet feed (packaging/ppa/make-source.sh)
dist/
vendor/
